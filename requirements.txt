anthropic>=0.40.0
pydantic>=2.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0
httpx>=0.27
# Optional provider experiment: DeepSeek via its OpenAI-compatible API
# (only used when ADAPTIVE_LEARNING_MODEL=deepseek-*).
openai>=1.50
